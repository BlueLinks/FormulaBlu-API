server:
    port: 8081

spring:
    application:
        name: races-api
        datasource:
            url: jdbc:postgresql://postgres-races:5432/formula1_races
            username: postgres
            password: password
        jpa:
            hibernate:
                ddl-auto: update
            show-sql: true
            properties:
                hibernate:
                    dialect: org.hibernate.dialect.PostgreSQLDialect

eureka:
    client:
        service-url:
            defaultZone: http://localhost:8761/eureka/
